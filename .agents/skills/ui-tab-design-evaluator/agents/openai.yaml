interface:
  display_name: "UI Tab Design Evaluator"
  short_description: "Score the UI tab against its target design"
  default_prompt: "Use $ui-tab-design-evaluator to evaluate whether the UI tab implementation matches the approved design."
